
Now I understand the structure. Let me implement `checkWin(state)` in `game.ts`.
Typecheck passes. Now let me check for existing tests to add a test for `checkWin`.
Now let me add tests for `checkWin` and update the import.
All 68 tests pass. Now let me update the TASKS.md checkbox and commit.
Done. Implemented `checkWin(state)` in `src/game/game.ts:262` — returns `true` when all 4 foundations have exactly 13 cards. Added 5 unit tests in `src/game/game.test.ts`. All 68 tests pass, typecheck is clean.